import React from "react";

function DisplayToday(weatherInfo) {
    // const tempMax = weatherInfo.main.temp_max;
    // const tempMin = Math.round(weatherInfo.main.temp_low - 32) * 5/9;

  return (
    <div>
      
    </div>
  );
}

export default DisplayToday;
